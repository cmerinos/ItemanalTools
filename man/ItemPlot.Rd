% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/itemPlot.R
\name{ItemPlot}
\alias{ItemPlot}
\title{itemPlot: Plot of item coefficients with confidence intervals}
\usage{
ItemPlot(
  data,
  item.col = "Item",
  value.col = "coef",
  lwr.ci.col = "lwr.ci",
  upr.ci.col = "upr.ci",
  group = NULL,
  threshold = NULL,
  sort.items = c("none", "asc", "desc"),
  orientation = c("h", "v"),
  theme.style = c("academic", "classic", "minimal"),
  show.values = FALSE,
  value.size = 3,
  value.digits = 2,
  value.format = c("estimate", "ci", "both"),
  value.hjust = 0.5,
  value.vjust = -0.6,
  xlim = NULL,
  point.size = 2.5,
  errorbar.width = 0.2,
  dodge.width = 0.5,
  point.color = "black",
  bar.color = "gray40",
  line.color = "red",
  xlab = NULL,
  ylab = NULL,
  title = NULL
)
}
\arguments{
\item{data}{A data.frame containing item-level results.}

\item{item.col}{Character. Name of the column containing item labels.}

\item{value.col}{Character. Name of the column containing the coefficient estimates.}

\item{lwr.ci.col}{Character. Name of the column containing the lower bound of the confidence interval.}

\item{upr.ci.col}{Character. Name of the column containing the upper bound of the confidence interval.}

\item{group}{Optional. Either:
\itemize{
\item A character string indicating a column in \code{data}, or
\item A vector of group labels with length equal to the number of rows in \code{data}.
}
When provided, coefficients and intervals are plotted separately by group.}

\item{threshold}{Optional numeric value. Adds a reference line (vertical or horizontal depending on orientation).}

\item{sort.items}{Character. Sorting method for items:
\describe{
\item{"none"}{Keeps original order.}
\item{"asc"}{Sorts items in ascending order of the coefficient.}
\item{"desc"}{Sorts items in descending order.}
}}

\item{orientation}{Character. Plot orientation:
\describe{
\item{"h"}{Horizontal (recommended).}
\item{"v"}{Vertical.}
}}

\item{theme.style}{Character. Plot theme:
\describe{
\item{"academic"}{Clean academic-style theme (default).}
\item{"classic"}{Classic ggplot2 theme.}
\item{"minimal"}{Minimal ggplot2 theme.}
}}

\item{show.values}{Logical. If TRUE, displays value labels on the plot.}

\item{value.size}{Numeric. Font size of value labels.}

\item{value.digits}{Integer. Number of decimal places in labels.}

\item{value.format}{Character. Format of value labels:
\describe{
\item{"estimate"}{Shows only the coefficient.}
\item{"ci"}{Shows only the confidence interval.}
\item{"both"}{Shows coefficient and confidence interval.}
}}

\item{value.hjust}{Numeric. Horizontal justification of value labels.}

\item{value.vjust}{Numeric. Vertical justification of value labels.}

\item{xlim}{Optional numeric vector of length 2. Sets the limits of the x-axis (for horizontal plots).}

\item{point.size}{Numeric. Size of the point representing the coefficient.}

\item{errorbar.width}{Numeric. Width (thickness) of the confidence interval bars.}

\item{dodge.width}{Numeric. Controls separation between groups.}

\item{point.color}{Character. Color of points (when no grouping is used).}

\item{bar.color}{Character. Color of confidence interval bars (when no grouping is used).}

\item{line.color}{Character. Color of the reference threshold line.}

\item{xlab}{Optional character. Label for x-axis.}

\item{ylab}{Optional character. Label for y-axis.}

\item{title}{Optional character. Plot title.}
}
\value{
A ggplot2 object.
}
\description{
Creates a graphical display of item-level coefficients with their corresponding
confidence intervals. The function supports optional grouping, item ordering,
reference thresholds, and customizable value labels.

This function is designed for outputs from item analysis procedures where each
row corresponds to an item and includes a coefficient estimate and its lower
and upper confidence interval bounds.
}
\details{
When \code{group} is provided, value labels (if enabled) are displayed for each
group-specific estimate. Labels are positioned over the corresponding confidence
interval lines to improve readability.

The horizontal orientation is recommended, especially when \code{show.values = TRUE},
as it improves label readability and avoids overlap.
}
\examples{
# Example without grouping
res <- data.frame(
  Item = paste0("Item", 1:5),
  coef = c(.62, .55, .48, .41, .36),
  lwr.ci = c(.54, .47, .39, .32, .27),
  upr.ci = c(.70, .63, .57, .50, .45)
)

itemPlot(res,
         show.values = TRUE,
         value.format = "both",
         threshold = 0.40)

# Example with grouping
res_g <- data.frame(
  Item = rep(paste0("Item", 1:5), 2),
  group = rep(c("A", "B"), each = 5),
  coef = c(.62, .55, .48, .41, .36,
           .58, .50, .45, .39, .33),
  lwr.ci = c(.54, .47, .39, .32, .27,
             .49, .42, .36, .30, .24),
  upr.ci = c(.70, .63, .57, .50, .45,
             .67, .58, .54, .48, .42)
)

itemPlot(res_g,
         group = "group",
         show.values = TRUE,
         value.format = "both",
         threshold = 0.40)

}
